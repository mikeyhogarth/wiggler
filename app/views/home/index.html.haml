%h1 Be Opinionated

%p Some kind of tagline that we'll think of later.

%table
  %thead
    %tr
      %th
      %th Name
      %th Description

    -@wiggles.each do |wiggle|
      %tr
        %td
          %canvas[wiggle]{:width => 50, :height => 50, :data=>{:average => wiggle.average_opinion}}
        %td= link_to wiggle.name, wiggle
        %td= wiggle.description

= content_for :page_specific_js do 
  -@wiggles.each do |wiggle|
    = subscribe_to wiggle.channel 

  :coffeescript    
    $ ->
      new HomePageReady()
